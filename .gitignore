# Ignore Gradle project-specific cache directory
.gradle

# Ignore Gradle build output directory
build

# Ignore Kotlin plugin data
.kotlin

# Build-Artefakt (wird per gradle build erzeugt)
agwcontrol.jar

# Serverkonfiguration – enthält interne Hostnamen, nicht einchecken
servers.properties

# KeePass-Datenbank – enthält Credentials, nicht einchecken
*.kdbx

# macOS Finder Metadaten
.DS_Store
**/.DS_Store
servers.kdbx.bak
reports/*
agwcontrol.db
app/report*.csv

/.bob/

# Alte jar-Extrakte (de/slackspace Quellen, Icons, Manifest) – nicht einchecken
/de/
/META-INF/
/icons/
